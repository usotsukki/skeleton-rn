# Agent / maintainer entry

- Human and AI **conventions**: [`.claude/rules/core-architecture.md`](.claude/rules/core-architecture.md) and companion rules under [`.claude/rules/`](.claude/rules/).
- **Expo + repo context**: [`CLAUDE.md`](CLAUDE.md).
- **Commands (verify, start, test)**: [`.claude/rules/commands.md`](.claude/rules/commands.md) — use **`yarn run check`** for lint + test on Yarn 1, not the built-in `yarn check`.
- **User-facing overview**: [`readme.md`](readme.md).

Do not duplicate the rule files here; update `.claude/rules/*.md` when behavior or layout changes.
